import 'package:flutter/material.dart';

class IndicatorItemImage extends StatelessWidget {
  const IndicatorItemImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
